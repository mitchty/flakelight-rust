[package]
name = "libonly"
version = "0.1.0"
edition = "2024"
license = "BlueOak-1.0.0"

[lib]
name = "libonly"
path = "src/lib.rs"

[dev-dependencies]
criterion = "0.8"

[[bench]]
name = "add"
harness = false
